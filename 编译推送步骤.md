检查是否可上传到 cdn
ping up-as0.qiniup.com

cd d:/TestWorkSpace/new-api/web
yarn build

编译：
docker buildx build --no-cache --platform linux/amd64 -t raoczh/new-api:custom-20260227-08 --load .
推送：
docker push raoczh/new-api:custom-20260227-08

修改 custom-20260227-08 版本号，在宝塔面板的容器编排中也同样修改
